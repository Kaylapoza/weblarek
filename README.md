# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные 
В приложении используются две сущности, которые описывают данные, — товар(IProduct) и покупатель(IBuyer).

#### Интерфейс IProduct
Основной интерфейс приложения. Используется везде, где есть взаимодействие с любым из свойств товара, либо самим товаром. Используется для учета товаров описанных в приложении.

Поля интерфейса:  
  `id: string;` - id продукта  
  `description:` string; - описание  
  `image: string;` - адрес изображения  
  `title: string;` - заголовок  
  `category: string;` - категория  
  `price: number | null;` - цена  


#### Интерфейс IBuyer 
Интерфейс покупателя. Используется для отправки формы на сервер при совершении покупки товаров. 
Определяет способы оплаты.

Поля интерфейса:  
  `payment: TPayment;` - оплата, может быть наличкой или картой  
  `email: string;` - адрес эл. почты  
  `phone: string;` - телефон  
  `address: string;` - адрес доставки  


#### Класс Catalog 
Отвечает за хранение товаров, которые можно купить в приложении.
Отвечает за хранение товаров, которые пользователь выбрал для покупки.

содержит в себе методы:  
  `saveProducts()`  сохранение массива товаров полученного в параметрах метода;  
  `getProducts()`  получение массива товаров из модели;  
  `getProductByID(id: string)`  получение одного товара по его id;  
  `saveCurrentProduct(product: IProduct | null)` сохранение товара для подробного отображения;  
  `getCurrentProduct()`  получение товара для подробного отображения.  

#### Класс Basket
Хранит массив товаров, выбранных покупателем для покупки.

Содержит в себе методы:  
  `getItems()`  получение массива товаров, которые находятся в корзине;  
  `addItem()`  добавление товара, который был получен в параметре, в массив корзины;  
  `removeItem()`  удаление товара, полученного в параметре из массива корзины;  
  `cleanBasket()`  очистка корзины;  
  `getTotalPrice()`  получение стоимости всех товаров в корзине;  
  `getItemCount()`  получение количества товаров в корзине;  
  `hasItemInBasket()`  проверка наличия товара в корзине по его id, полученного в параметр метода.  

#### Класс Buyer 
Хранит в себе вид оплаты, адрес доставки, телефон и email.

Содержит в себе методы:  
  `setBuyerData(buyer: Partial<IBuyer>)`  сохранение данных в модели. Один ощий метод  
  `getBuyerData()`  получение всех данных покупателя;  
  `clearBuyerData()`  очистка данных покупателя;  
  `validate()`  валидация данных. Метод, возвращающий объект с ошибками.  

### Слой коммуникации
#### Класс RequestService
Данный класс отвечает за отправку данных на сервер и за получение данных с сервера. Используется функциональность класса API 
Поля класса:  
  `api: IApi;` использует интерфейс запросов IApi  

Класс исплользует композицию и работает с методами класса API.

Методы:  
  `getProducts(): Promise<IProduct[]>` метод get класса API используется для получения с серера объекта с массивом товаров.  
  `postOrder(order: OrderRequest)` метод post класса API используется для отправки данных пользователя, полученных в аргументах, на сервер  

### Представление

#### Интерфейс IModal  
Интефейс класса Modal

Поля интерфейса:  
`content: HTMLElement`

#### Класс Modal
Компонент для управления модальным окном. Он не привязан к конкретному контенту и служит лишь «рамой», которая умеет открываться, закрываться и менять содержимое.

Конструктор: 
`constructor(container: HTMLElement, events: IEvents)` - принимает элемент модального окна в DOM, а также брокер событий для уведомления системы об открытии или закрытии окна.

Поля класса:
`content: HTMLElement` - элемент-контейнер внутри модального окна, куда будет вставляться динамическая разметка.
`closeButton: HTMLButtonElement` - кнопка закрытия окна.

Методы:
`set content(value: HTMLElement)` - заменяет текущее содержимое модального окна новым элементом.
` open()` - открывает модалку
` close()` - закрывет модалку

#### Класс Card<T>
класс работает с базовым шаблоном и общими для всех товаров данными.

Конструктор:  
`constructor(container: HTMLElement, events: IEvents)` - container: Элемент карточки, склонированный из темплейта, events: брокер событий для передачи сигналов в Презентер.  

Поля класса:  
`title: HTMLElement` - название товара  
`price: HTMLElement` - цена товара  

Методы:   
`set title(value: string)` - устанавливает текст в элемент заголовка  
`set price(value: number | null)` - выводит значение стоимости

#### Интерфейс ICard  
Базовый интерфейс карточки с общими полями

Поля интерфейса: 
`id: string` - id товвара  ???
`title: string`- заоловок  
`price: number | null` -  цена товара (может быть равна 0);

#### Интерфейс ICatalogCard extends ICard
Интерфейс для дочернего класса CardCatalog, расширяет базовый интерфейс карточки.  

Поля интерфейса:  
`category: string` - категория товара  
`image: string` -  ссылка на изображение  

####  Класс CatalogCard  
Предназначен для отображения товара на главной странице. Наследует Card<ICatalogData> 

Методы:  
`set category(value: string)` - устанавливает категорию товара  
`set image(value: string)` -  устанавливает ссылку на изображение в DOM

#### Интерфейс ICardPreview extends ICatalogCard  
Интерфейс для дочернего класса CardPreview. Расширяет интерфейс ICatalogCard 

Поля интерфейса:  
`description: string` - подробное описание товара

#### Класс CardPreview  
Детальное отображение товара в модальном окне. Наследует: Card<ICardPreview>  

Методы:  
`set buttonText(value: string)` - устанавливает текст кнопки
`set valid(value: boolean)` - делает кнопку активной или неактивной
`set description(value: string)` - отображает описание товара  

#### Интерфейс ICardBasket extends ICard  
Интерфейс карточки внутри корзины. Расширяет базовый интерфейс карточки. 

Поля интерфейса: 
`index: number` -  номер товара в списке корзины

#### Класс CardBasket  
Элемент списка внутри корзины. Наследует: Card<IBasketData>  

Методы:  
`set index(value: number)` - устанавливает номер товара в списке корзины.

#### ИНтерфейс IForm
Этот интерфейс описывает состояние любой формы в приложении.

Поля интерфейса:  
`valid: boolean` - показыает правильность заполненой формы
`errors: string[]` - массив со списком ошибок

#### Класс Form<T>  
Этот класс управляет разметкой внутри тега <form>. Он наследует Component<IForm>  

Конструктор:  
`constructor(container: HTMLFormElement, events: IEvents)` - находт кнопку сабмита, ешает слушатели событий на форму  

Поля класса:  
`submit: HTMLButtonElement` - кнопка подтверждения  
`errors: HTMLElement` - элемент куда выводятся ошибки  

Методы:  
`set valid(value: boolean)` - управляет свойством disabled у кнопки сабмита.  
`set errors(value: string)` - устанавливает текст ошибок в нужный элемент.  
`onInputChange(field: keyof T, value: string)` - генерирует событие для презентера об изменениях в форме


#### Интерфейс IOrderForm  
Интерфейс первого шага (выбор оплаты и адрес).  

Поля интерфейса:  
`payment: 'card' | 'cash' | null` - способ оплаы cash или card  
`address: string` - Адрес доставки  

#### Класс Order  
Первый этап — способ оплаты и адрес. Наследует: Form<IOrderForm>.  

Поля:  
`buttons: HTMLButtonElement[]` - массив из двух кнопок («онлайн» и «наличными»).  

Методы:  
` set payment(value: 'card' | 'cash' | null)` - устанавливает способ оплаты
`set address(value: string)` - сеттер для адреса доставки

#### Интерфейс IContactsForm  
Интерфейс второго шага (контакты).  

Поля интерфейса:  
`email: string;`
`phone: string;`

#### Класс Contacts  
Второй этап — почта и телефон. Наследует: Form<IContactsForm>.  

#### Интерфейс IBasket  
Интерфйс корзины.

Поля интерфейса:  
`items: HTMLElement[]` - список отрисованных карточек  
`total: number` - общая сумма в корзине
`active: boolean` - отвечает за активность кнопки оформить

#### Класс  Basket  
Этот класс управляет списком выбранных товаров в модальном окне. Принимает Component<IBasket>

Конструктор:  
`constructor(container: HTMLElement, events: IEvents)` - Находит в шаблоне список (ul), элемент для итоговой цены и кнопку оформления заказа.

Поля класса:  
`listElement: HTMLElement` — тот самый список <ul>, куда будут вставляться карточки товаров.  
`totalElement: HTMLElement` - текстовый элемент для отображения итоговой суммы  
`buyButton: HTMLButtonElement` - кнопка «Оформить»  

Методы:  
`set items(items: HTMLElement[])` - Если массив не пуст, заменяет содержимое списка элементами из массива. Если пуст — выводит текст «Корзина пуста» и блокирует кнопку оформления.  
`set total(total: number)` - Устанавливает значение итоговой суммы.  
`set active(value: boolean)` - дизаблер кнопки

#### Класс Success  
Окно успешной оплаты. ПРинимает Component<ISuccess>

Конструктор:  
`constructor(container: HTMLElement, events: IEvents)` - Находит кнопку «За новыми покупками!» и элемент для вывода итоговой списанной суммы.  

Поля класса:  
`close: HTMLButtonElement` - кнопка «За покупками»  
`total: HTMLElement` - сумма покупок  

Методы:  
`set total(value: number)` - устанавливает сумму покупок в тексте финального сообщения  

#### Интерфейс ISuccess
Поля интерфейса: 
`total: number;` - сумма списания